package parser;

public class CSVParser implements FileParser {
    @Override
    public void parseFile(String inputFilePath, String outputFilePath) {
        // Implement parsing logic for CSV files
        // Use CreditCardFactory and CreditCard classes for processing
    }
}
